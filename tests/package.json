{
  "name": "lca-login-tests",
  "version": "1.0.0",
  "description": "Playwright tests for LCA Healing Playground login functionality",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
