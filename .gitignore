.DS_Store
.playwright-mcp/

# TM pusher scripts + creds — kept locally, deliberately NOT published:
# this repo is public via GitHub Pages and the case text cites internal
# tickets/subsystems (TC-942, abstentionPipeline, AIAutoHealer, spec detail).
.bsauth
.bsproj
tm/

# Local test-case exports — same reason as above: internal emails, PR refs
# (pr-1946/pr-4486), Flipper/LCNC subsystem names. Never published.
browser-flags-test-cases.csv
*-test-cases.csv
node_modules/
test-results/

# Test Companion onboarding (local, per-machine)
.test-companion/
